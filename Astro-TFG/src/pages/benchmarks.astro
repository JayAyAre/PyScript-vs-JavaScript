---
import '../styles/global.css';
import Layout from '../layouts/BenchmarkLayout.astro';

const benchmark = {
    title: 'Benchmarks',
    link: '',
    description:
        'A continuacion, se listan los distintos apartados de cada una de las pruebas, que pueden variar teniendo versiones o no de la misma prueba.',
};
---

<Layout {...benchmark} />
